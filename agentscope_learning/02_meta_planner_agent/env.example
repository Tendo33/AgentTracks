# ==============================================================================
# Meta Planner Agent Configuration
# ==============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file with actual API keys to version control
# ==============================================================================

# ==============================================================================
# Required API Keys
# ==============================================================================
# OpenAI API key for model access
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Tavily API key for search functionality
# Get from: https://tavily.com
TAVILY_API_KEY=tvly-your-tavily-api-key-here

# ==============================================================================
# Optional Model Configuration
# ==============================================================================
# Base URL for OpenAI API (useful for proxies or alternative endpoints)
# Default: https://api.openai.com/v1
# OPENAI_BASE_URL=https://api.openai.com/v1

# Model name to use for both planner and worker agents
# Default: gpt-4-turbo
# Options: gpt-4-turbo, gpt-4, gpt-3.5-turbo, or any compatible model
# CHAT_MODEL=gpt-4-turbo

# Model temperature (controls randomness in responses)
# Default: 0.7
# Range: 0.0 (deterministic) to 2.0 (very random)
# MODEL_TEMPERATURE=0.7

# Maximum tokens per generation
# Default: 32000
# MODEL_MAX_TOKENS=32000

# Enable streaming responses (true/false)
# Default: true
# MODEL_STREAM=true

# ==============================================================================
# Optional Agent Configuration
# ==============================================================================
# Custom name for the agent
# Default: Task-Meta-Planner
# AGENT_NAME=Task-Meta-Planner

# Custom working directory for agent file operations
# If not set, defaults to ./meta_agent_demo_env
# All agent file operations will be confined to this directory
# AGENT_OPERATION_DIR=/path/to/custom/working/directory

# Directory to save agent states for recovery
# Default: ./agent-states
# AGENT_STATE_SAVING_DIR=./agent-states

# Maximum reasoning-action iterations for the planner
# Default: 100
# AGENT_MAX_ITERS=100

# Maximum iterations for worker agents
# Default: 20
# AGENT_WORKER_MAX_ITERS=20

# ==============================================================================
# Optional Planner Configuration
# ==============================================================================
# Planning mode for the agent
# Default: dynamic
# Options:
#   - disable: Only use simple ReAct mode (no planning)
#   - dynamic: Automatically switch between ReAct and planning based on task
#   - enforced: Always use planning mode for all tasks
# PLANNER_MODE=dynamic

# ==============================================================================
# Optional Tool Configuration
# ==============================================================================
# Maximum characters in tool responses (to prevent overwhelming the model)
# Default: 40970 (approximately 40KB)
# TOOL_RESPONSE_BUDGET=40970

# ==============================================================================
# Optional Logging Configuration
# ==============================================================================
# Logging level
# Default: DEBUG
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=DEBUG

# Enable logfire logging (true/false)
# Default: false
# ENABLE_LOGFIRE=false

# ==============================================================================
# Optional MCP (Model Context Protocol) Configuration
# ==============================================================================
# NPX command for MCP clients
# Default: npx
# MCP_NPX_COMMAND=npx

# Tavily MCP package name
# Default: tavily-mcp@latest
# MCP_TAVILY_PACKAGE=tavily-mcp@latest

# Filesystem MCP package name
# Default: @modelcontextprotocol/server-filesystem
# MCP_FILESYSTEM_PACKAGE=@modelcontextprotocol/server-filesystem

# ==============================================================================
# Notes
# ==============================================================================
# 1. Lines starting with # are comments
# 2. Remove the # at the beginning of a line to uncomment it
# 3. Environment variables can also be set in your shell or system settings
# 4. Values set in the shell take precedence over values in .env file
# 5. Always keep your API keys secure and never commit them to version control

